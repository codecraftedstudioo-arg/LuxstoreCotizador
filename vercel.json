{
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "{server/**,data/**}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
