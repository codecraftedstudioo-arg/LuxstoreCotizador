# Variables de entorno locales: NO subir al deploy.
# El build de Vercel usa las env vars del proyecto (dashboard/CLI), no estos archivos.
# Esto evita que un valor local (ej. VITE_PANEL_API_URL de pruebas) se filtre a prod.
.env
.env.*
